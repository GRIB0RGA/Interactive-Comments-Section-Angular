export enum ScoreBtnType {
  Plus = '+',
  Minus = '-',
}

export enum CommentingTypes {
  Comment = 'comment',
  Edit = 'edit',
  Replay = 'replay',
}
export enum BtnTypes {
  Comment = 'send',
  Edit = 'update',
  Replay = 'replay',
}
