export enum ScoreBtnType {
  Plus = '+',
  Minus = '-',
}
export enum VoteStatus {
  Upvoted = 1,
  Neutral = 0,
  DownVoted = -1,
}

export enum CommentingTypes {
  Comment = 'comment',
  Edit = 'edit',
  Replay = 'replay',
}
export enum BtnTypes {
  Comment = 'send',
  Edit = 'update',
  Replay = 'replay',
}
